import React from "react";
import Layout from "@/components/Layout";

export default function PartenariatsPage() {
  return (
    <Layout>
      <section className="mt-6">
        <h1 className="text-2xl font-semibold text-white mb-2">Partenariats</h1>
        <p className="text-white/80 text-sm">
          Page en cours de construction. Le contenu détaillé (texte, sections, animations)
          pourra être intégré ici à partir des maquettes que nous avons définies.
        </p>
      </section>
    </Layout>
  );
}
